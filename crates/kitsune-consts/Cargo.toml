[package]
name = "kitsune-consts"
edition.workspace = true
version.workspace = true
build = "build.rs"

[dependencies]
const_format = "0.2.32"

[build-dependencies]
vergen = { version = "8.2.6", features = ["build", "git", "gitcl"] }
