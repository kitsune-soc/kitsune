[package]
name = "just-retry"
authors.workspace = true
edition.workspace = true
version.workspace = true
license = "MIT OR Apache-2.0"

[dependencies]
retry-policies = "0.4.0"
tokio = { version = "1.39.1", features = ["time"] }
tracing = "0.1.40"

[lints]
workspace = true
