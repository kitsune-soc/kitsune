[package]
name = "kitsune-http-signatures"
version = "0.1.0"
edition = "2021"

[dependencies]
base64 = "0.20.0"
http = "0.2.8"
ring = { version = "0.16.20", features = ["std"] }
thiserror = "1.0.38"

[features]
